# SPS 与 SynergiGas 迁移技能入库检查

检查日期：2026-09-24。范围：用户提供的两个 ZIP 内的全部文件、入库调整、离线测试及分发包结构。

## 结论

两个原包均通过 skill-creator 的基础格式检查；入库调整后通过仓库校验。必需的名称、描述、入口文件与相对资源引用齐全，技能名称和目录一致，正文远低于 500 行。

**未发现实际工程案例文件或可识别的现场数据。** 原包包含 27 个文件，全部可作为 UTF-8 文本读取；只有说明、配置、许可证、Python 辅助代码、测试及校验清单。文档和测试包含少量明确标注为人工合成的参数与微型拓扑，不属于“完全没有数据”的空模板。

这是对所提供文件的内容审查结论，无法仅凭文本独立证明所有示例的历史来源。未执行实际工程转换、MDB 驱动验证、平台导入或求解；不据此宣称工程精度或客户端兼容性通过。

## 原包与来源追踪

| 原包 | 文件数 | SHA-256 |
| --- | --- | --- |
| sps-to-nexusflow-v1.0.0.zip | 14 | `a2ff701f504b060566efc3b76908a3b7effeafc8aff2ef55b372660606f1e23b` |
| synergigas-to-nexusflow-1.0.0.zip | 13 | `754cc9616fca315802d36c196c623902d36c4f8558391fa0017f57569ce9936b` |

SPS 原包 `MANIFEST.sha256` 中的 13 项文件哈希全部匹配。原包版本是来源信息；本仓库的集合版本仍为 `0.1.0`（未发布），没有据此生成新的上游 `1.0.0` 发行版。

## 数据检查

| 技能 | 存在的示例数据 | 未发现的材料 |
| --- | --- | --- |
| SPS | `references/helpers.md` 和 `scripts/test_conversion_tools.py` 中的 A/B 组分、取整分子量、两点压缩机曲线、简短 RAMP、N1/N2 端口图、压力及状态插值样例 | 实际 INPREP、INTRAN、OUTPRP、OUTTRN、REVIEW、RESTRT 文件，工程结果、截图、现场机组曲线文件 |
| SynergiGas | `references/audit-contract.md` 和 `tests/test_audit_model.py` 中的 n1/n2、p1、b1/b2 两节点单管模型，2000 年示例时间轴和简化边界值 | MDB/ACCDB 数据库、CSV/XLSX 测点表、实际模型导出、坐标表或现场记录 |

所有说明和源代码均已阅读。检查了 URL、IP、邮箱、本机绝对路径、UUID、疑似凭据赋值等模式，原包中未检出匹配项。未见网络调用、外部下载、凭据读取或平台写入的执行代码；SPS 测试中的子进程仅运行同包离线工具，并使用临时合成文件。

## 规范问题与入库调整

| 项目 | 原状 | 处理 |
| --- | --- | --- |
| 命名 | 两个原名称符合开放规范，但不满足仓库最初的 `nexusflow-` 前缀约定 | 保留原名称和调用方式，允许 `<source>-to-nexusflow` 命名并同步贡献规范 |
| 运行条件 | 正文已有 Python、读取驱动和平台要求，入口没有 `compatibility` | 补充中文 `compatibility`，区分离线工具和平台操作要求 |
| 许可证 | 两个包都有 MIT LICENSE；SynergiGas 入口未声明 | 补充 `license: MIT`，保留两个原 LICENSE 及版权声明 |
| 默认语言 | SPS README 首段和许可范围说明为英文 | 改为中文；代码接口、工具错误消息和 MIT 原文保持原样 |
| 使用边界 | SynergiGas 描述已有迁移边界，但缺少明确反例 | 补充触发示例和不应自动上传、求解的情形 |
| 文件哈希 | SPS 带有原包哈希清单 | 原包先验证；修改后重新生成入库文件的清单，避免保留过期哈希 |
| 持续验证 | 原包各自有离线测试 | 将两组测试加入仓库校验和草稿发布工作流 |

原有转换算法、公式和测试代码保持不变。未将压缩包、实际案例、私有临时目录或运行产物加入技能目录。

## 验证记录

环境：Windows，Python 3.12.14。

- 两个原包：skill-creator 基础校验通过。补充 `compatibility` 后，本机附带的旧版检查器报告未知字段；当前 [Agent Skills 规范](https://agentskills.io/specification) 已将该字段列为可选字段，仓库检查器也支持。保留该字段，不把旧工具的结果记为通过，也不据此判为现行规范错误。
- 仓库元数据、命名和本地链接检查：2 个技能通过。
- SPS 离线测试：15 项通过。
- SynergiGas 离线测试：8 项通过。
- 仓库维护工具测试：5 项通过。
- 两个单技能包与完整包：完成打包检查、哈希核验及单包解压后的离线测试。

附带脚本分别是有限的 SPS 数据转换辅助工具和 SynergiGas 中间模型结构检查器。它们不是完整的厂商解析器，也不自动生成适用于所有 NexusFlow 版本的可运行算例。

## 后续验证

拿到获授权的源模型与目标平台后，再验证读取驱动、实际组件定义、接口、客户端任务执行和工程结果；将结果补充到 [兼容性记录](../compatibility.md)。公开仓库仍只保留合成复现材料。
