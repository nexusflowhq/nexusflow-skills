# 贡献指南

欢迎通过问题单（Issue）提出需求、报告问题，通过合并请求（Pull Request）贡献技能和修复。

## 默认语言

使用文档、技能说明、问题模板和更新记录默认使用简体中文，新增内容也应遵循这一约定。
技能的 `description` 和正文默认使用中文；标准字段名、技能标识、文件名、命令、API 名称及许可证原文保留原文。
如需提供其他语言版本，应保持与中文版本同步。

## 新增技能

1. 先说明目标用户、触发场景和预期输出，避免与已有技能重复。
2. 阅读 [编写规范](docs/authoring.md)，创建 `skills/nexusflow-<task>/SKILL.md`；迁移类技能可使用 `skills/<source>-to-nexusflow/SKILL.md`。
3. 将必要的脚本、参考资料、模板放入该技能目录。不要依赖其他技能已安装。
4. 在 README 技能目录中登记，并更新 [兼容性记录](docs/compatibility.md)。
5. 为正常、异常和不应触发的场景提供输入、预期结果与验证记录。
6. 更新 `CHANGELOG.md` 的“未发布”部分，运行校验和相关测试。

```bash
python -m pip install -r requirements-dev.txt
python scripts/validate.py
python -m unittest discover -s tests -v
```

## 审查要求

- 说明触发条件、前置条件、步骤、成功标准及失败处理。
- 声明平台、SDK/MCP 和客户端要求；未经验证时标记为“未验证”。
- 脚本遵循平台真实权限，不通过提示词代替服务端鉴权。
- 提交前人工检查凭据、私有地址、客户数据和第三方授权。当前自动检查不替代完整的敏感信息扫描。
- 修改外部数据的技能须说明影响，遵循用户授权和客户端确认机制。
- 只提交与本次任务有关的修改，不提交本地产物或生成的 ZIP。

## 维护约定

所有技能共用仓库版本。维护者通过 PR 审查合并，按 [发布流程](docs/releasing.md) 创建发行版。
客户端适配包由同一份源码生成；不要复制多份技能正文分别维护。
贡献者应确认其提交内容允许按本仓库许可证分发；第三方内容保留原始许可。
