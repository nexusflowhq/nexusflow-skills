# 验证约定

`test_tooling.py` 验证仓库维护工具的关键行为，使用临时目录，不会在 `skills/` 中创建示例技能。

后续每个技能的场景材料放在 `tests/<skill-name>/`，记录：

- 应触发和不应触发的用户请求。
- 正常输入、预期结果及成功证据。
- 权限不足、无效输入、超时或任务失败等异常。
- 平台、客户端、SDK/MCP 版本与验证日期。

外部系统测试使用明确授权的测试环境；普通 PR 检查不依赖生产凭据。
